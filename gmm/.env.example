# ============================================================================
# Arquivo de Configuracao - Sistema GMM
# Gestao Moderna de Manutencao
# ============================================================================
#
# INSTRUCOES:
# 1. Copie este arquivo para .env
# 2. Preencha os valores necessarios
# 3. NUNCA compartilhe o arquivo .env (ele contem informacoes sensiveis)
#
# ============================================================================

# ----------------------------------------------------------------------------
# CONFIGURACOES DO FLASK
# ----------------------------------------------------------------------------

# Chave secreta para sessoes (OBRIGATORIO)
# Gerar com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=sua-chave-secreta-muito-aleatoria-aqui-mude-isso-obrigatorio

# Ambiente de execucao (development ou production)
FLASK_APP=run.py
FLASK_ENV=development

# ----------------------------------------------------------------------------
# BANCO DE DADOS
# ----------------------------------------------------------------------------

# OPCAO 1: SQLite (desenvolvimento/pequenas instalacoes)
# Banco de dados local em arquivo
DATABASE_URL=sqlite:///instance/gmm.db

# OPCAO 2: PostgreSQL (producao/grandes instalacoes)
# Descomente e configure para usar PostgreSQL
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/gmm
#
# Exemplo com host remoto:
# DATABASE_URL=postgresql://gmm_user:senha123@192.168.1.50:5432/gmm_db

# ----------------------------------------------------------------------------
# REDIS (CELERY)
# ----------------------------------------------------------------------------

# URL do servidor Redis para Celery (broker de mensagens)
CELERY_BROKER_URL=redis://localhost:6379/0

# URL do servidor Redis para resultados do Celery
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Para Redis remoto ou com autenticacao:
# CELERY_BROKER_URL=redis://:senha@192.168.1.50:6379/0
# CELERY_RESULT_BACKEND=redis://:senha@192.168.1.50:6379/0

# ----------------------------------------------------------------------------
# WHATSAPP - MEGAAPI (OBRIGATORIO PARA NOTIFICACOES)
# ----------------------------------------------------------------------------

# Chave de API do MegaAPI WhatsApp
# Obtenha em: https://megaapi.com.br
MEGA_API_KEY=sua-chave-api-megaapi-aqui

# URL da API MegaAPI (geralmente nao precisa alterar)
MEGA_API_URL=https://api.megaapi.com.br/v1/messages

# ----------------------------------------------------------------------------
# CRIPTOGRAFIA (OBRIGATORIO)
# ----------------------------------------------------------------------------

# Chave Fernet para criptografar credenciais sensiveis no banco
# Gerar com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=sua-chave-fernet-32-bytes-base64-aqui

# ----------------------------------------------------------------------------
# EMAIL (OPCIONAL - PARA NOTIFICACOES POR EMAIL)
# ----------------------------------------------------------------------------

# Servidor SMTP
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True

# Credenciais de email
# Para Gmail, use uma "Senha de App": https://support.google.com/accounts/answer/185833
MAIL_USERNAME=seu-email@gmail.com
MAIL_PASSWORD=sua-senha-de-app-gmail

# Email remetente padrao
MAIL_DEFAULT_SENDER=seu-email@gmail.com

# Email do departamento de compras (para notificacoes de pedidos)
PURCHASE_EMAIL=compras@suaempresa.com

# ----------------------------------------------------------------------------
# SLACK (OPCIONAL - PARA ALERTAS)
# ----------------------------------------------------------------------------

# Webhook URL do Slack para alertas de sistema
# Obtenha em: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/seu/webhook/aqui

# ----------------------------------------------------------------------------
# OPENAI (OPCIONAL - PARA PROCESSAMENTO DE LINGUAGEM NATURAL)
# ----------------------------------------------------------------------------

# Chave de API da OpenAI (para funcionalidades de NLP)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-sua-chave-openai-aqui

# ----------------------------------------------------------------------------
# CONFIGURACOES AVANCADAS (OPCIONAL)
# ----------------------------------------------------------------------------

# Nivel de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Tamanho maximo de upload em MB
MAX_CONTENT_LENGTH=16

# Timeout de sessao em minutos
PERMANENT_SESSION_LIFETIME=60

# Modo de debug do Flask (NUNCA use True em producao!)
# DEBUG=False

# ----------------------------------------------------------------------------
# NOTAS IMPORTANTES
# ----------------------------------------------------------------------------
#
# 1. SEGURANCA:
#    - NUNCA commite o arquivo .env no Git
#    - Adicione .env ao .gitignore
#    - Use permissoes restritas: chmod 600 .env
#
# 2. VALORES OBRIGATORIOS (minimo para funcionar):
#    - SECRET_KEY
#    - FERNET_KEY
#    - DATABASE_URL (ja tem padrao SQLite)
#
# 3. VALORES RECOMENDADOS:
#    - MEGA_API_KEY (para WhatsApp)
#    - Configuracoes de email (para notificacoes)
#
# 4. PRODUCAO:
#    - Altere FLASK_ENV para production
#    - Use PostgreSQL em vez de SQLite
#    - Configure HTTPS (certificado SSL)
#    - Configure proxy reverso (Nginx/Apache)
#    - Use senhas fortes e unicas
#
# 5. GERAR CHAVES:
#    SECRET_KEY:
#      python -c "import secrets; print(secrets.token_hex(32))"
#
#    FERNET_KEY:
#      python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#
# ============================================================================
